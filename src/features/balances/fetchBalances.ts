import { logger } from 'src/utils/logger'

export async function fetchBalances(address: string) {
  logger.debug('Fetching balances for:', address)
  // TODO merge in Spencer's implementation
  return '0'
}
