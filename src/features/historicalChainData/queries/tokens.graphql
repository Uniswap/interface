# Queries token data in bulk
query tokens($tokenList: [ID!]!) {
  tokens(
    where: { id_in: $tokenList }
    orderBy: totalValueLockedUSD
    orderDirection: desc
    subgraphError: allow
  ) {
    id
    derivedETH
  }
}
