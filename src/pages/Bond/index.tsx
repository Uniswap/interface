import { RouteComponentProps } from 'react-router-dom'

// import AppBody from '../AppBody'

export default function Bond({ history }: RouteComponentProps<{ currencyIdA?: string; currencyIdB?: string }>) {
  return (
    // <AppBody>
    <p>bond</p>
    // </AppBody>
  )
}
