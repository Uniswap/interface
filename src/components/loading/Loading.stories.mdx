import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { HeaderLoader } from './HeaderLoader'
import { BoxLoader } from './BoxLoader'
import { GraphLoader } from './BoxLoader'

<Meta title="Components/Loading" component={BoxLoader} />

export const Template = (args) => <HeaderLoader {...args} />

# Loading

`Loading` is a placeholder for loading states. Note that the shimmer effect isn't displayed here because web doesn't support an alternative for `MaskView`, which the Shimmer component uses :'(

<Canvas>
  <Story name="default" args={{}}>
    <BoxLoader />
  </Story>
</Canvas>

<Canvas>
  <Story name="header" args={{}}>
    <HeaderLoader />
  </Story>
</Canvas>

<ArgsTable story="default" />
