/** An ES module wrapper for @uniswap/widgets. */

// eslint-disable-next-line no-restricted-imports
import widgets from '@uniswap/widgets/cjs'

export const SwapWidget = widgets.SwapWidget
export const SUPPORTED_LOCALES = widgets.SUPPORTED_LOCALES

export default widgets
