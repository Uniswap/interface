{
  "buildCommand": "yarn build",
  "devCommand": "yarn start",
  "installCommand": "yarn install --ignore-engines --registry https://registry.yarnpkg.com",
  "framework": null,
  "outputDirectory": "build",
  "env": {
    "NODE_VERSION": "20.x"
  }
}
