/// <reference types="uniwind/types" />

// Pulls in uniwind's React Native type augmentations (adds `className` to View,
// Text, ScrollView, Image, etc.). uniwind also generates a uniwind-types.d.ts on
// first Metro run for theme-name typing; this file is the static augmentation that
// makes `className` typecheck without needing Metro to have run.
