// same as tsconfig.json but without references which caused performance issues with typescript-eslint
{
  "extends": "./tsconfig.json",
  "references": [],
  "include": ["src/**/*", ".maestro/scripts/**/*.ts", "__mocks__/**/*", "scripts/**/*", "*.ts", "*.js", "*.d.ts"]
}
