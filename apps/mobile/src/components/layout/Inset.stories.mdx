import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box, Flex, Inset } from './'
import { Text } from '../Text'

<Meta
  title="WIP/Layout/Inset"
  component={Inset}
  decorators={[
    (Story) => (
      <Box bg="background3" borderRadius="rounded12">
        <Inset>
          {/* calls function directly to preserve displayName */}
          {Story()}
        </Inset>
      </Box>
    ),
  ]}
/>

export const Template = (args) => (
  <Box bg="accentAction">
    <Inset {...args}>
      <Box bg="accentAction">
        <Text color="white">This is the default inset</Text>
      </Box>
    </Inset>
  </Box>
)

# Inset

<Canvas>
  <Story name="default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

`Inset` offers spacing _around_ children. Think of it as a picture frame aroundn a set of children.

<Canvas>
  <Story name="variants" args={{}}>
    <Flex>
      {['spacing8', 'spacing12', 'spacing16', 'lg', 'xl'].map((size) => (
        <Box bg="black">
          <Inset all={size}>
            <Box bg="background3">
              <Text>This is the {size} inset</Text>
            </Box>
          </Inset>
        </Box>
      ))}
    </Flex>
  </Story>
</Canvas>

<ArgsTable story="default" />
