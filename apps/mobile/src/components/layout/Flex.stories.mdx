import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box, Flex, Inset } from './'

<Meta
  title="WIP/Layout/Flex"
  component={Flex}
  decorators={[
    (Story) => (
      <Box bg="surface1" borderRadius="rounded12">
        <Inset>
          {/* calls function directly to preserve displayName */}
          {Story()}
        </Inset>
      </Box>
    ),
  ]}
/>

export const Template = (args) => <Flex {...args} />

# Flex

`Flex` offers horizontal or vertical spacing _between_ children.

<Canvas>
  <Story name="default" args={{ centered: true, flexDirection: 'column', gap: 'spacing12' }}>
    {Template.bind({})}
  </Story>
</Canvas>

`Flex` exposes the following:

- `centered` as a shorthand for `alignItems='center' justifyContent='center'`
- `gap`: spacing between children as defined in theme
- and every `BoxProps`

<Canvas>
  <Story name="args" args={{ centered: true, flexDirection: 'row', gap: 'lg' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
