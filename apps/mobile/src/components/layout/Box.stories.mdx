import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box } from './'
import { Text } from '../Text'

<Meta title="WIP/Layout/Box" component={Box} />

export const Template = (args) => <Box {...args} />

# Box

`Box` is the most low-level layout component. Its job is to render an individual element on the screen.

For example, if you wanted to create a container element with small spacing on all sides:

<Canvas>
  <Story
    name="Box"
    args={{
      children: <Text color="neutral1">Lorem ipsum dolor sit amet</Text>,
      backgroundColor: 'surface1',
      borderRadius: 'spacing16',
      padding: 'spacing16',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Box" />
