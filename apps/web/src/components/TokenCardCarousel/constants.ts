import { spacing } from 'ui/src/theme'

/** Horizontal gap between carousel cards. */
export const CAROUSEL_CARD_GAP = spacing.spacing12

/** Width of left/right edge fade overlays. */
export const CAROUSEL_FADE_WIDTH = 96

/** Narrow fade overlay width on small viewports (<=450px). */
export const CAROUSEL_FADE_WIDTH_SMALL = 24

/** Max card width on small viewports (<=450px). */
export const CAROUSEL_SMALL_CARD_MAX_WIDTH = 280
