name: '[mobile] Build Checks'

on:
  workflow_call:

  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    name: Build check
    runs-on: ubuntu-latest
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
    defaults:
      run:
        working-directory: ./

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: 📥 Install
        uses: ./.github/actions/monorepo_yarn_install

      - name: 🏗  Build
        run: yarn g:build

      - name: 🛍️ Test Bundle
        run: yarn mobile ios:bundle
