/**
 * The option-list word-prefix filter — re-exported from the shadcn Command
 * recipe layer (`../shadcn/filter`), the single source of truth for the
 * legacy NetworkFilterV2 matcher. Pure and platform-neutral: real on BOTH
 * platform legs.
 */
export { normalizeOptionSearchQuery, optionMatchesSearchQuery, type SearchableOptionFields } from '../shadcn/filter'
