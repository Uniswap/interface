# Teleswap

# pre todo
change `.env.example` to `.env` and fill your personal secret key in it.
# init enviroment 
`yarn`
# compile contract
`yarn compile`
# export abi
yarn hardhat export-abi
# run test
`yarn hardhat test`
