// Reuse the uniswap package's full test environment: RTL-for-RNTL mock,
// IntersectionObserver stub, and the shared package mocks (config presets,
// environment, utilities, uniswap, ui). The moved passkey tests were written
// against exactly that environment.
import '../uniswap/vitest-setup'
