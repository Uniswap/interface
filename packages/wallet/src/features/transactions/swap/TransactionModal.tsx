import {
  TransactionModalFooterContainerProps,
  TransactionModalInnerContainerProps,
  TransactionModalProps,
} from 'wallet/src/features/transactions/swap/TransactionModalProps'

export function TransactionModal(_: TransactionModalProps): JSX.Element {
  throw new Error('Implemented in `.native.tsx` and `.web.tsx` files')
}

export function TransactionModalInnerContainer(
  _: TransactionModalInnerContainerProps
): JSX.Element {
  throw new Error('Implemented in `.native.tsx` and `.web.tsx` files')
}

export function TransactionModalFooterContainer(
  _: TransactionModalFooterContainerProps
): JSX.Element {
  throw new Error('Implemented in `.native.tsx` and `.web.tsx` files')
}
