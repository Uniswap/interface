import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box } from '../layout/Box'
import { Text } from '../text/Text'
import { Button, ButtonEmphasis, ButtonSize } from './Button'

<Meta title="WIP/Button" component={Button} />

export const Template = (args) => <Box padding='$spacing16' backgroundColor='$surface2' borderRadius='$rounded4'><Button {...args} /></Box>

# Button

_Add description..._

<Canvas>
  <Story
    name="Button"
    args={{
      children: 'Click me',
      buttonEmphasis: ButtonEmphasis.Primary,
      buttonSize: ButtonSize.Medium,
      onClick: () => alert('clicked')
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />
