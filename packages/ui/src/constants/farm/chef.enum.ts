export enum Chef {
  MASTERCHEF,
  MASTERCHEF_V2
  // MINICHEF
}

export enum Rewarder {
  SIMPLE,
  COMPLEX,
  ALCX
}

export enum PairType {
  SWAP,
  KASHI,
  TRIDENT
}

export enum OnsenModalView {
  Liquidity,
  Staking,
  Position,
  AddLiquidityConfirm
}
