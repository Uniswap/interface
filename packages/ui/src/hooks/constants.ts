import { spacing } from 'ui/src/theme'

export const DEFAULT_BOTTOM_INSET = spacing.spacing20

// Disabling lint rules for PascalCase enum Member name as IPhoneSE feels wrong
export enum MobileDeviceHeight {
  iPhoneSE = 667,
  iPhone12 = 812,
}
