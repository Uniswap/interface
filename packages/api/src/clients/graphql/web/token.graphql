query TokenWeb(
  $chain: Chain!
  $address: String = null
  $multichain: Boolean = false
  $preferProjectMarketData: Boolean = false
) {
  token(chain: $chain, address: $address) {
    id
    decimals
    name
    chain
    address
    symbol
    standard
    market(currency: USD, multichain: $multichain) {
      id
      totalValueLocked {
        id
        value
        currency
      }
      price {
        id
        value
        currency
      }
      volume24H: volume(duration: DAY, multichain: $multichain) {
        id
        value
        currency
      }
      priceHigh52W: priceHighLow(duration: YEAR, highLow: HIGH) {
        id
        value
      }
      priceLow52W: priceHighLow(duration: YEAR, highLow: LOW) {
        id
        value
      }
    }
    project {
      id
      name
      description
      homepageUrl
      twitterName
      logoUrl
      isSpam
      tokens {
        id
        chain
        address
        market(currency: USD) @include(if: $multichain) {
          id
          totalValueLocked {
            id
            value
            currency
          }
          price {
            id
            value
            currency
          }
          volume24H: volume(duration: DAY) {
            id
            value
            currency
          }
        }
      }
      markets(currencies: [USD]) {
        id
        price {
          id
          value
          currency
        }
        fullyDilutedValuation {
          id
          value
          currency
        }
        marketCap {
          id
          value
          currency
        }
        priceHigh52W: priceHighLow(duration: YEAR, highLow: HIGH) {
          id
          value
        }
        priceLow52W: priceHighLow(duration: YEAR, highLow: LOW) {
          id
          value
        }
        pricePercentChange24h {
          id
          value
        }
        volume24H: volume(duration: DAY) @include(if: $preferProjectMarketData) {
          id
          value
          currency
        }
      }
    }
  }
}

query TokenProjectWeb($chain: Chain!, $address: String = null) {
  token(chain: $chain, address: $address) {
    id
    decimals
    name
    chain
    address
    symbol
    standard
    project {
      id
      description
      homepageUrl
      twitterName
      logoUrl
      isSpam
      tokens {
        id
        chain
        address
      }
    }
  }
}
